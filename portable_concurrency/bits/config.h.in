#cmakedefine HAS_CONDITIONAL_NOEXCEPT
#cmakedefine HAS_STD_ALIGNED_UNION

#if defined(HAS_CONDITIONAL_NOEXCEPT)
#  define NOEXCEPT_IF(cond) noexcept(cond)
#else
#  define NOEXCEPT_IF(cond) noexcept
#endif
